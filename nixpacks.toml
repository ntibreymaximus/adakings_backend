[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements-prod.txt"
]

[phases.build]
cmds = [
    "echo 'Build phase complete - static files and migrations will run at startup'"
]

[start]
cmd = "bash railway_start.sh"
