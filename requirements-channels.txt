# Additional requirements for WebSocket support
channels==4.0.0
channels-redis==4.1.0
daphne==4.0.0

# If Redis is not available, channels will use InMemoryChannelLayer in development
# For production, configure Redis URL in environment variables
